require 'spec_helper'

describe JiraManpower do
  it 'has a version number' do
    expect(JiraManpower::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
